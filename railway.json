{
  "$schema": "https://railway. app/railway- schema. json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "PORT=3 000 cd server && node server. js",
    "healthCheckPath": "/api/ health"
  }
}